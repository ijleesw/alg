#include "prelude.h"

#define IN(x) ::std::cin >> x
#define DEFIN(x) ll x; IN(x)

constexpr int INF = INT_MAX / 2;
constexpr int MOD = 1e9 + 7;
constexpr int xs[4] = {-1, 0, 0, 1};
constexpr int ys[4] = {0, -1, 1, 0};

using namespace std;
using ll = long long;
using pii = pair<int, int>;
using ivec = vector<int>;

int main() {
  ds();

  return 0;
}
